{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.scripts.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023"],
    "types": ["node"],
    "skipLibCheck": true,

    /* scripts は tsx で実行する。src と同じく @/ エイリアスと拡張子なしの import を使うため bundler 解決にする */
    "module": "esnext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    "paths": {
      "@/*": ["./src/*"]
    },

    /* Type checking */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  /*
   * scripts から import した src のファイルも、この設定（DOM なし、types: node）で型チェックされる。
   * scripts から import してよいのは、DOM や import.meta.env に依存しないモジュール
   * （src/content/themeIds.ts、src/lib/i18n/locale.ts、src/lib/i18n/messages/）に限る
   */
  "include": ["scripts/**/*.ts"]
}
